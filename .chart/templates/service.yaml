apiVersion: v1
kind: Service
metadata:
  name: {{ include "ReviewPhin.fullname" . }}
  labels:
    {{- include "ReviewPhin.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ReviewPhin.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: worker
