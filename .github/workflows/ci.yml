name: CI

on:
  pull_request:

permissions:
  contents: read

jobs:
  quality-gates:
    name: quality-gates
    uses: ./.github/workflows/quality-gates.yml
    permissions:
      actions: read
      contents: read
      pull-requests: read
      security-events: write
    with:
      coverage-artifact-name: coverage
